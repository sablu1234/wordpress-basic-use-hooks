function myplugin_settings() {
    if(! current_user_can('manage_options') && (! wp_doing_ajax())){
        wp_die(__("Your are not to allow to access this part"));
    }
}
add_action( 'admin_init', 'myplugin_settings' );
